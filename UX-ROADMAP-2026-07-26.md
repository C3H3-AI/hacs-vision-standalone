# HACS Vision · 用户视角后续版本规划（便利性 / 美观性）

> 配套文档：`AUDIT-OPTIMIZATION-FEASIBILITY-2026-07-26.md`（工程视角审计）
> 规划日期：2026-07-26
> 视角：用户使用便利性、美观性、移动端体验、信息架构
> 关键约束：当前前端 `frontend/panel.js` 为 641KB 压缩黑盒，**仓库无 TS/JSX 源码**。因此「便利性」类可纯后端/配置落地；「美观性」类必须先做前端源码化（见工程报告 P2-1）或另起新面板。

---

## 0. 用户现状画像（基于已知事实）

- 主要入口：HA 侧边栏「HACS」（面板 URL `/hacs-vision`）
- 主力使用设备：**手机 + 无痕模式**（用户实测场景）—— 对认证注入、首屏空白极敏感
- 核心使用场景：集成管理、仓库管理（安装/更新/查看）
- 已有能力：原版 HACS 界面开关（`use_classic_ui`）；预发布探测；自动更新；实体引用查找；README 翻译

---

## A. 便利性（后端 / 配置层即可做，无需前端源码）

> 这些不依赖前端改代码，改动风险低，可并入 `v6.7`。

| # | 能力 | 用户价值 | 落地方式 | 工作量 |
|---|------|---------|---------|--------|
| A1 | **移动端加载状态提示** | 避免「首屏空白 = 坏了」的误判（用户刚踩的坑） | 后端加 `/api/hacs_vision/health` 探活；前端若黑盒则靠 HTTP 状态码 + 文案兜底（需前端源码化后才完美） | 0.5 天（后端）+ 前端需源码 |
| A2 | **更新可用 → HA 持久通知** | 不用每次进面板翻，有更新主动提醒 | 自动更新模块扩展：检测到新版本发 `persistent_notification` | 1 天 |
| A3 | **失效仓库自动检测** | 仓库 404/被归档时主动告知，而非更新才报错 | `hacs_operator` 加校验任务 + 通知 | 1 天 |
| A4 | **批量操作（多选安装/更新）** | 一次管多个仓库，省点击 | 后端加批量端点（复用现有幂等锁） | 1-2 天 |
| A5 | **设置入口合并** | 现在两套开关（`use_classic_ui` + `hide_hacs_panel`）易混淆 | 统一到 config_entry 选项流（工程报告 P0-3） | 1 天 |
| A6 | **一键健康检查入口** | 普通用户也能看懂「现在正不正常」 | 后端聚合版本/缓存/上游状态 → 简单 JSON 面板 | 0.5 天 |

---

## B. 美观性（需前端源码化 / 新面板）

> 以下都必须先解决「前端黑盒」问题（工程报告 P2-1），否则只能跟上游。

| # | 能力 | 用户价值 | 前置条件 | 工作量 |
|---|------|---------|---------|--------|
| B1 | **跟随 HA 主题 / 暗色模式** | 与 HA 整体视觉一致，护眼 | 前端源码化 + CSS 变量接入 HA 主题 | 1-2 天（前端） |
| B2 | **卡片化信息架构** | 集成管理 / 仓库管理分区更清晰，找东西快 | 前端源码化 or 新面板 | 2-3 天 |
| B3 | **状态可视化** | 更新徽标、版本对比、预发布标记一眼可见 | 前端源码化 | 1-2 天 |
| B4 | **移动端响应式布局** | 手机上不再挤、好点 | 前端源码化 or 新面板 | 2-3 天 |

---

## C. 版本路线图（按用户价值排序）

| 版本 | 主题 | 包含 | 依赖 |
|------|------|------|------|
| **v6.7** | 便利性补强 | A2 / A3 / A4 / A5 / A6（后端为主） | 无前端源码依赖 |
| **v6.8** | 前端源码化 + 视觉 | P2-1 引入构建 + B1 / B3 | 需引入前端源码仓 + Vite |
| **v6.9** | 响应式 + 新面板（可选） | B2 / B4，或自研轻量面板彻底脱离上游 UI | 视 v6.8 结果决定 |

---

## D. 与工程报告的衔接

- A5 / A6 与工程报告 P0-3 重合 → 合并到「阶段 1」一起做
- B 系列全部依赖 P2-1（前端源码化）→ 必须先立项 P2-1，B 才有基础
- 上游 fork 漂移（P1-3）会持续影响前端黑盒的「跟上游」路线——若选 B 系列自研路线，反而降低对上游 UI 的依赖

---

## E. 需要你拍板的决策点

1. **v6.7 便利性补强现在就做吗？** —— 纯后端、零前端风险，我建议做。
2. **美观性路线选哪条？** —— (a) 引入前端源码跟上游并改；(b) 直接自研新面板（彻底掌控 UI，但工作量大）。
3. **前端源码化（P2-1）是否立项？** —— 这是 B 系列所有美观性改进的前置，必须二选一先解决。

> 下一步：你确认 v6.7 范围后，我直接改后端（A2/A3/A4/A5/A6），部署到 `api.homediy.top` 验证，再请你点头发布。
